export enum FrameMessageType {
  DATA       = 'data',
  DICTIONARY = 'dictionary',
  INIT       = 'init',
  LANG       = 'lang',
  LOOKUP     = 'lookup',
  OPTIONS    = 'options',
  VALIDATION = 'validation',
}
